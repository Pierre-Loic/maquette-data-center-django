"""Modèle pour pi_credentials.py (fichier réel non commité, voir .gitignore).

Copier ce fichier en pi_credentials.py à côté de control_panel.py et
renseigner les vrais identifiants SSH, identiques sur les 3 Raspberry Pi.
"""

SSH_USER = "miai"
SSH_PASSWORD = "change-me"
